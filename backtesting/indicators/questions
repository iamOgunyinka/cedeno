*   Should all the indicators have the same data base, or every indicator should have its own data base.
    Because maybe some datas have a different frequency
*   Will some test be implemented with the creed and bear data base and other directly with binance?
*   It is possible to swtich between static and dynamic mode? If so, we need to reset the queue?
*   How is buyer vs seller when the time is reached and with need to start the substraction.

BWFS    
    - Type static or dinamic
    - Variable used
    - Time frame minutes
    - client limit

BUYER VS SELLER only use when it is dymanic

* Parameter functions of my callback
* A global variable to access through python
* A friendly way to call indicator init
* time theshold
